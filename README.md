A collection of ready to use components, insipired by many React UI library. Built on Svelte 5, with Bits UI and Tailwind.

Currenty porting DiceUI components.

# Build

```bash
pnpm install
pnpm run dev
```
